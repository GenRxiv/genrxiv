<IfModule mod_rewrite.c>
	RewriteEngine On

	# Route requests that don't match real files/dirs through index.php
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteRule ^(.*)$ index.php/$1 [L,QSA]
</IfModule>
